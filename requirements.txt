# Pinned exactly. An unpinned build resolves whatever is newest on PyPI at
# deploy time, so a dependency's major release can break production with no
# commit of ours. Bump these deliberately, with CI green.
flask==3.1.3
flask-sqlalchemy==3.1.1
flask-migrate==4.1.0
flask-wtf==1.3.0
flask-login==0.6.3
gunicorn==26.2.0
psycopg2-binary==2.9.13
python-dotenv==1.2.3
boto3==1.43.95
anthropic==1.6.0
elevenlabs==2.68.0
requests==2.34.2
croniter==6.2.4
google-auth==2.58.0
