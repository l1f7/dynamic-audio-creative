"""Desktop push mode — files dropped into a watched folder become delivered runs.

The contract this package implements lives in the ctn-dropd repo
(``docs/dac-api.md``). The Rust daemon deserialises these responses
strictly, so field names and status codes here must not drift.
"""
