# Dynamic Audio Creative

## Bugs
- Cron doesn't always run or misses scheduled runs. I'm still unsure on how the cron knows its time to run.

- (fixed, needs test) DV360 submission or creation seems to run out of memory (thats what the log says), how can we reduce mem usage for these submissions?

- (fixed, needs test) Script repeats certain phrases, make sure the script and scores generated by AI are done before attaching the custom creative {ad_tag}. Currently, the ad tag is part of the AI prompt, but just concat after. Order should be, fetch scores, create script, pick the ad copy from available options and append to script, send to eleven labs.

## Features
- way to delete/archive runs from the system

## Tweaks
- reduce logging to avoid logging large responses.
